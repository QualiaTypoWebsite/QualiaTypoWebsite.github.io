# Vite 8 / Rolldown import `styleText` from node:util, which only exists on
# Node 20.19+. Refuse to install on older Node rather than leaving a
# half-populated node_modules that fails later with a cryptic error.
engine-strict=true
